//readPayment.service.js:

//Función getUserPaymentStatus(userId): (Lógica: Llama a usersRepo.getUserProfileFromDB(userId) y devuelve solo el campo paymentStatus).

//Exportar { getUserPaymentStatus }.